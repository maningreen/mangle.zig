# mangle.zig

mangle.zig is a game engine structured around metadata tags, structure recomposition, and comptime processing.

## Getting started

To use mangle.zig, create a new zig project and fetch the latest mangle.zig version
```
zig init
```
